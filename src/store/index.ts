import { createStore } from 'vuex'
//import { notificationInfo } from './notifications/notifications';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
   // notificationInfo
  }
})
